export const EXTENSION_NAME = "pi-better-grok";
export const CONFIG_BASENAME = "pi-better-grok.json";
export const STATUS_KEY = "better-grok";

export function logPrefix(): string {
  return `[${EXTENSION_NAME}]`;
}
